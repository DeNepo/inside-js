// #todo

'use strict';

let userInput = '';
while (userInput === '') {
  /* -- BEGIN: validate input -- */
  /* -- END: validate input -- */
}

const finalMessage =
  '"' + userInput + '" is ' + userInput.length + ' characters long';
alert(finalMessage);
